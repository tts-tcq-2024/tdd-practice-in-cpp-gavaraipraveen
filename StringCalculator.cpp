#include "StringCalculator.h"
#include <iostream>
#include <string>

class StringCalculator {
public:
    int add(const std::string& numbers) {
        if (numbers.empty()) {
            return 0;
        }
    }
};
